## 0.1.6

### Fixed
- Полностью переписан ввод значений в `install.sh`.
- Убрана схема с command substitution для секретов.
- Значения теперь пишутся в переменные через `printf -v`, без попадания переводов строки в `.env`.
- `.env.example` и `docker-compose.yml.example` снова нормальные многострочные файлы.
