# Changelog

## 0.1.8
- Исправлен запуск Kong через отдельный `kong-entrypoint.sh`.
- В установку включены реальные `kong.yml` и `kong-entrypoint.sh`.
- Studio наружу больше не публикуется; доступ через Kong, локально только `127.0.0.1:3000`.
- Контейнеры `auth`, `rest`, `meta`, `studio` названы так, как ожидает `kong.yml`.
- Починен интерактивный ввод без поломки `.env`.
