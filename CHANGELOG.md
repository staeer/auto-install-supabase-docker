## 0.1.1

### Changed
- Исправлен сценарий вопросов в `install.sh`.
- Для режима IP убран дефолт `127.0.0.1`: теперь IP/hostname нужно ввести явно.
- Вопрос `Disable signup (true/false)` заменён на `Разрешить регистрацию новых пользователей? [Y/n]`.
- `install.sh` и `.env.example` приведены к нормальному многострочному виду.

### Fixed
- Убраны кривые однострочные файлы в репозитории.
- Улучшена запись `.env` и проверка обязательных шаблонов.
