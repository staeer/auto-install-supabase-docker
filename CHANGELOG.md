## 0.1.4

### Fixed
- Полностью переписан ввод через `/dev/tty` без `read -p` в command substitution.
- Исправлен баг, из-за которого секреты записывались на следующую строку и ломали `.env`.
- Нормализована генерация случайных значений без скрытых переводов строки.

## 0.1.3

### Fixed
- Исправлен `ask_secret`: ввод секретов теперь не ломает `.env`.
- Ввод выполняется через `/dev/tty`, чтобы prompt и перевод строки не попадали в переменные.
- `install.sh`, `.env.example` и `docker-compose.yml.example` сделаны нормальными многострочными файлами.
- Для IP режима убран дефолт `127.0.0.1`, адрес нужно ввести явно.
- Вопрос про регистрацию новых пользователей переведён в формат `[Y/n]`.
