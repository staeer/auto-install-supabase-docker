## 0.1.7

### Changed
- Установщик больше не блокирует установку по маркеру `PLACEHOLDER_REPLACE_ME`.
- Проверяются только наличие обязательных файлов в `volumes/db` и `volumes/api`.
- Обновлён README под архив с реальными файлами из репозитория Supabase.

### Fixed
- Сохранена рабочая логика интерактивного ввода без поломки `.env`.
