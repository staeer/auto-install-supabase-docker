## 0.1.2

### Fixed
- Исправлена запись секретов в `.env`: `SERVICE_PASSWORD_POSTGRES`, `SERVICE_PASSWORD_JWT` и `SERVICE_PASSWORD_ADMIN` больше не улетают на отдельные строки.
- Исправлен `ask_secret`: перевод строки теперь не ломает захват значения.
- Исправлен блок установки зависимостей: `python3` и `openssl` ставятся только при отсутствии.

### Changed
- Для режима IP поле `IP или hostname сервера` теперь обязательное и без дефолта `127.0.0.1`.
- Вопрос `Disable signup (true/false)` заменён на человеческий: `Разрешить регистрацию новых пользователей? [Y/n]`.
- Добавлен `.env.example`.
